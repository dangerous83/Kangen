# Netlify build settings
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: BrowserRouter needs every path served from index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
